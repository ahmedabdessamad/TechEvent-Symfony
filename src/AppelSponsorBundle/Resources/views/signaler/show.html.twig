{% extends 'base.html.twig' %}

{% block body %}
    <h1>Signaler</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ signaler.id }}</td>
            </tr>
            <tr>
                <th>Sponsor</th>
                <td>{{ signaler.sponsor }}</td>
            </tr>
            <tr>
                <th>Club</th>
                <td>{{ signaler.club }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ signaler.description }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('signaler_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('signaler_edit', { 'id': signaler.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
