{% extends 'base.html.twig' %}

{% block content %}
    <div class="top-breadcrumb-area bg-img bg-overlay d-flex align-items-center justify-content-center" style="background-image: url({{ asset('img/bg-img/24.jpg') }});">
    <h1>Appelsponsor edit</h1>
    </div>

    {{ form_start(edit_form) }}
    <div class="row">
        <div class="col-md-6">
            <h1><b>Dossiersponsoring edit </b></h1>
        </div>
        <div class="col-md-6 text-right">
            <br>
            <button type="submit" class="btn btn-success" value="Edit">
                <i class="fa fa-check" aria-hidden="true"></i> Confirmer
            </button>
            <a  href="{{ path('appel_sponsor_index') }}" class="btn btn-secondary">
                <i class="fa fa-undo" aria-hidden="true"></i>  Retour à la liste</a>

            <br>
        </div>
    </div>
    {#{% form_theme form 'bootstrap_3_layout.html.twig' %}#}
    {% form_theme edit_form 'bootstrap_3_layout.html.twig' %}

    {{ form_widget(edit_form) }}

    {{ form_end(edit_form) }}


            {{ form_start(delete_form) }}
                <input type="submit" value="Delete" class="btn btn-danger">
            {{ form_end(delete_form) }}


{% endblock %}
